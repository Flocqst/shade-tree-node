node_modules/
*.log
tor/data/
tor/hs/
tor/tor.pid
.secret
.env
tor/tor-pow/
tor/tor-pow-build/
tor/data-client/
tor/tor-client.pid
shim-bogus.log
keys.local.json
demo-keys.local.md
group/directory-signer.key
*.lkg
docs/walkthrough.live.html
out/
cache/
broadcast/
contracts/deployed.local.json

# bootnode + gateway onion identities and signer keys (secret)
identity.local.json
**/identity.local.json
# Rust client member identity file from `rgoe identity` ({identitySecret, leaf}) — secret
identity.json
**/identity.json
bootnode/bootnode-signer.key
hs_ed25519_secret_key

# bootnode write-through persistence (runtime state, not source)
bootnode/bootnode-state.json
**/bootnode-state.json
coverage/

# Mutation testing (T-TEST-10) — Stryker sandbox + HTML report are disposable
.stryker-tmp/
reports/mutation/

# go-live receipts (contain droplet/laptop IPs) and encrypted key backups (.rgoebak) — local only
golive/
backups/
contracts/paid-access.local.json
